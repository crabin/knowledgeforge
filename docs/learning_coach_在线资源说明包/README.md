# 资料包说明

本文件夹为独立整理后的在线资源说明包。

目录结构：
- ./learning_coach_在线资源获取代码与逻辑说明.md
- ./code/coach.py
- ./code/content_pusher.py
- ./code/online_resource.py
- ./code/cache/ （运行副本代码后会在这里生成缓存）

说明：
1. 文档中的代码路径已统一改为相对路径。
2. code/ 下是从原始工程复制出的副本，便于单独阅读。
3. 其中 online_resource.py 的默认缓存路径已改为相对路径 `./code/cache` 对应的位置。
4. 原始源码仍保留在：/home/crabin/satadisk/hermes_save/learning_coach
